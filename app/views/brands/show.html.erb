<div class="jumbotron">
	<div class="container">
	<div class="panel panel-primary">
		<div class="panel-heading"><%=t('global.menu.brand')%></div>
	  <div class="panel-body">
			<div class="container">
				<div class="row">
					<div class="col-lg-6 col-md-6 col-sm-6">
						<span><strong><%=t('global.nom')%> : </strong><%=@brand.name%></span><br />
					</div>
				</div>
			</div>	
		</div>
	</div>
</div>
	<div class="row">
		<%= link_to t('global.edit'), edit_brand_path(@brand), :class => "btn btn-default" %>
		<%= link_to t('global.back'), :brands, :class => "btn btn-default" %>
	</div>
</div>
