%h1 Listing user_attachments

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @user_attachments.each do |user_attachment|
      %tr
        %td= link_to 'Show', user_attachment
        %td= link_to 'Edit', edit_user_attachment_path(user_attachment)
        %td= link_to 'Destroy', user_attachment, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User attachment', new_user_attachment_path
